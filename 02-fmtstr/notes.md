# notes

For the format string challenge we want to show how to properly abuse format
string specifiers.

Need to create a stack thats easy to inspect meaning its obvious to tell
what the stack looks like by filling it with a repeating pattern of bytes.

Create a script that shows how to dump process memory / arbitrary reads.

Create a script that shows how to arbitrarily write to any address.
